{
  "extends": "stylelint-config-recommended",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["each", "for", "function", "if", "include", "mixin"]
      }
    ]
  }
}
